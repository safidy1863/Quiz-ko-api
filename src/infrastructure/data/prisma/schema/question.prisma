enum QuestionType {
    SINGLE
    MULTIPLE
    OPEN
}
model Question {
  id          String    @id @default(uuid())
  title       String
  description String
  point       Float
  type        QuestionType
  answers     Answer[]   
  subjects    SubjectQuestions[]
}

generator client {
  provider = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://quiz-ko:quiz-ko_password@localhost:5432/quiz-ko?schema=public"
}